{
  "name": "demoscene-neon-clocktower",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "vite",
    "headless": "npm run build && node scripts/headless-render.mjs",
    "headless:matrix": "npm run build && node scripts/headless-matrix.mjs",
    "headless:lighting": "npm run build && node scripts/headless-lighting-regression.mjs",
    "headless:phase": "npm run build && node scripts/headless-phase-regression.mjs",
    "headless:eclipse": "npm run build && node scripts/headless-eclipse-regression.mjs",
    "headless:clocktower-lighting": "npm run build && node scripts/headless-clocktower-lighting.mjs",
    "headless:latitude": "npm run build && node scripts/headless-latitude-regression.mjs",
    "headless:camera": "npm run build && node scripts/headless-camera-regression.mjs",
    "headless:ui": "npm run build && node scripts/headless-ui-regression.mjs",
    "headless:test-phase": "npm run headless:matrix && npm run headless:lighting && npm run headless:clocktower-lighting && npm run headless:latitude && npm run headless:ui",
    "headless:debug": "npm run build && node scripts/headless-render.mjs --debug",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "type": "module",
  "devDependencies": {
    "playwright": "^1.58.2",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "three": "^0.183.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
